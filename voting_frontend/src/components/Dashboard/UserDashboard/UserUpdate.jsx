import React from "react";

const UserUpdate = () => {
  return (
    <button className="btn btn-warning mt-2" onClick={() => alert("Update feature coming soon!")}>
      Update Info
    </button>
  );
};

export default UserUpdate;
